(executables
  (names test parser-unit-test)
  (libraries str calc oUnit))

(alias
  (name runtest)
;  (deps (source_tree ./data))
  (action
    (progn (run ./test.bc)
    (run ./parser-unit-test.bc))
  )
)
